{
  "name": "@webdecoy/clearance-worker",
  "version": "0.1.0",
  "private": true,
  "description": "WebDecoy clearance edge validator (FR7) — Cloudflare Worker verifying wd_clearance tokens",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250712.0",
    "typescript": "^5.5.0"
  }
}
