{
  "name": "@webdecoy/clearance-lambda",
  "version": "0.1.0",
  "private": true,
  "description": "WebDecoy clearance edge validator (FR7 for AWS) — Lambda@Edge verifying wd_clearance tokens",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.5.0"
  }
}
